server:
  name: im_server                #service名字
  listen: ":8081"                #服务监听地址端口
  timeout: 1000                  #请求最长处理时间 单位 毫秒

db:
  dsn: ""
  max_idle_conns: 10
  max_open_conns: 100
  max_life_time: 3600